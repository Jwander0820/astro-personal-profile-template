# Optional: use this only for a custom domain.
# SITE_URL=https://example.com

# Optional: public keeps /studio/ in deployed output; off removes the route
# and footer link. Local `npm run studio` remains available in either case.
# ONLINE_STUDIO_MODE=public

# Optional: public keeps /studio/ in deployed output; off removes the route
# and footer link. Local `npm run studio` remains available in either case.
# ONLINE_STUDIO_MODE=public
